car = ["tomatoes", "Patata", "Lechuga"];
//para afecto de performance
car.map((producto) => {
  return "hola" + producto;
});
// const applu = document.getElementById("app");
// console.log(applu);
// let carrito =
car.map((producto) => {
  return "hola" + producto;
});

// console.log(carrito);
// pegamos a la consola para comprobar

// ObjectKeys   pasando un objeto traemos el objeto aveces tenemos conclusion si la llave existe
const objetKey = { name: "robert", edad: 45 };

console.log(Object.keys(objetKey.name));
